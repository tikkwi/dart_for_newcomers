// This is a dummy JavaScript file to demonstrate Dart-JS interop.

// A simple function that shows an alert
function showJsAlert(message) {
  alert("JS Alert: " + message);
}

// A function that performs a calculation and returns a result
function calculateSum(a, b) {
  console.log("Calculating sum in JS...");
  return a + b;
}
